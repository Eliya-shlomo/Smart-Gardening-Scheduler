apiVersion: v1
kind: Service
metadata:
  name: audit
  labels:
    app: audit
spec:
  type: ClusterIP
  selector:
    app: audit
  ports:
    - name: http      
      port: 8000
      targetPort: 8000
